# AWS credentials for local Terraform usage
# Copy this file to `config` and fill in your values:
#   cp config.example config
#
# Then load it into your shell before running Terraform:
#   source ./config
#
# Do not commit `config` — it is gitignored. Better secret management
# (IAM roles, AWS SSO, Secrets Manager, CI OIDC) will come later.

export AWS_ACCESS_KEY_ID="YOUR_ACCESS_KEY_ID"
export AWS_SECRET_ACCESS_KEY="YOUR_SECRET_ACCESS_KEY"
export AWS_DEFAULT_REGION="eu-west-1"

# Optional: use a named profile instead of access keys
# export AWS_PROFILE="teacher-wang"

# Optional: Google SSO for Cognito (both required to create the Google IdP).
# 1. Create a Google Cloud OAuth 2.0 Web client (APIs & Services → Credentials).
# 2. Authorized redirect URI (after Cognito exists):
#      cd environments/prod && terraform output -raw cognito_google_redirect_uri
# 3. Uncomment and set both values, then: terraform apply
#    Expect cognito_google_enabled = true afterwards.
# export TF_VAR_cognito_google_client_id="YOUR_GOOGLE_OAUTH_CLIENT_ID"
# export TF_VAR_cognito_google_client_secret="YOUR_GOOGLE_OAUTH_CLIENT_SECRET"
# Alternative: put JSON {"client_id":"...","client_secret":"..."} in Secrets Manager
# and export TF_VAR_cognito_google_oauth_secret_arn="arn:aws:secretsmanager:..."

